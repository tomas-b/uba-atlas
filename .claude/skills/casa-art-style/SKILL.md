---
name: casa-art-style
description: (LATER — not yet active) Generate a CASA node's artistic render from its grounded books/DNA. Sibling of Synergy-Shock's catalog-style-transfer; multi-modal (photo / game texture / text-to-3D). Art comes after grounding — do not use until a node is grounded to L2+ and Tomas activates this layer.
---

# casa-art-style — deferred

**Not active yet.** Grounding (`casa-grounding`) comes first; a node needs L2 (its real
book references) before it can be rendered, because the aesthetic is extracted *from the
books*. Full design: `/p/learn/PROJECT.md` (L5).

## When it turns on

Adapt Synergy-Shock's **`catalog-style-transfer`** skill (fal.ai Seedream v4; structured DNA
schema `aesthetic.json` [lighting/kelvin/direction/shadow, composition/depth_of_field/framing]
+ `color.json`; a 4-asset style firewall; a Change/Keep final-prompt contract).

**Mapping:** brand → materia · curated IG feed → the materia's real source books · per-brand
envelope → per-materia DNA + a shared house style. **One adaptation:** catalog preserves a
real product (image 0); CASA generates the hero *from the DNA/concept* (no image 0) — flip
the terminal stage edit-preserve → generate-from-DNA; everything upstream transfers.

## The vibe is the index; modality is downstream

One grounded DNA drives multiple heads: **photo** (navigator cards, "now" feed) · **game
texture** (surfaces for the study-house — the L5→L7 seam) · **text-to-3D** (props). Same
fail-hard law at every head: the vibe must trace to the real books.
