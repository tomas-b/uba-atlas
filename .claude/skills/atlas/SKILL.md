---
name: atlas
description: Everything UBA Atlas — expand the graph to a career ("expandí Edición"), ground one materia, adversarially verify a node, measure coverage, query the data, build and deploy. One skill; role docs live next to this file.
---

# atlas — one skill, four roles

## Mental model (always)

```
UBA → career (L1: official plan) → course (L2: cátedra syllabus) → book (L3: leaf)

to index ──research──▶ to create ──generate──▶ created
```

- 🌱 **Expansion** = research pushes the frontier down one level.
- ⚔️ **Verification** = an adversary attacks every new leaf before it ships.
- 🕳️ No source → **seal it honestly**; the gap is information.
- 📦 One wave = one career = one commit = one revert point.

## Pick your role, read its doc

| you were asked to… | read | announce with |
|---|---|---|
| expand a career / run or close a wave | `wave.md` | `🌊 atlas ▸ wave` |
| ground ONE materia or career node | `grounding.md` | `✍️ atlas ▸ grounding: <target>` |
| refute ONE node | `verify.md` | `⚔️ atlas ▸ verify: <address>` |
| coverage · query the data · build/deploy | `ops.md` | `📊 atlas ▸ ops` |

Announce first (bash `echo`), then work. Reports: short, markdown lists,
emojis as status (✅ ❌ 📄 🔢). Future layer (not live): generative art per
node — see `PROJECT.md`.
